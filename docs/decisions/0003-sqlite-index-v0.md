# ADR 0003: SQLite index for v0

## Status
Accepted

## Decision
Use SQLite as the first index layer.

## Why
SQLite is simple to ship, easy to inspect, supports atomic updates, and is adequate for a local single-writer prototype.
